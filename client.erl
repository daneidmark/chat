-module(client).

-export([join/2]).

join(_Ip, _Port) ->
    % Connect to remote host

    % Provide a valid user name

    % Create a ServerActor (pst, use spawn to create a new process)

    % Create a input listener (pst, take a look at io:get_line)
    ok.

